import { Header } from "../Header"

type MainProps = {

}
export const Main = (props: MainProps) => {
    return (
        <div className=" bg-primary-1000 min-h-full p-5">
            <Header />
        </div>
    )
}

